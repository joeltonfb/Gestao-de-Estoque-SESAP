# Credenciais — nunca versionar
config.ini

# Planilhas geradas a cada execução (o site já leva os dados dentro do HTML)
saida/

# Planilhas soltas na raiz (restos da estrutura antiga)
/*.xlsx
/*.xls
/*.xlsm

# O catálogo mestre É versionado de propósito: é entrada fixa do programa,
# e assim o servidor recebe tudo que precisa com um único "git clone".
!dados/BASE_DADOS_COMPLETA.xlsx

# Backups locais do painel original
*.BACKUP-*.html
*.BACKUP-*.js

# Restos da exportação original do artifact
extracted/
uploads/
.thumbnail
support.js

# Python
__pycache__/
*.pyc
.venv/
venv/
